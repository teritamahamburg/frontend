query ($text: String!){
    searchItems(text: $text) {
        id
        schoolName
        name
        code
        partId
        amount
        purchasedAt
        user {
            name
        }
        course {
            name
        }
        checkedAt
        room {
            number
        }
        disposalAt
        depreciationAt
        editUser {
            name
        }
        createdAt
        deletedAt
    }
}
